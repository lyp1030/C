#include <stdio.h>
//#include-包含一个叫<stdio.h>(函数头文件)调用库函数必须有头文件
//stdio.h-std-标准 standard input输入 output输出
int main()//主函数-程序的入口-main函数有且仅有一个
//int整形
//main函数前面的int表示main函数调用返回一个整形值
{
	printf("Hello, world\n");//库函数print f打印函数-格式("打印内容")\ n下一行
	return 0;//返回 0
}
